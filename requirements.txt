# ATS Resume Scorer — combined backend + frontend dependencies.
#
# Install with:
#     pip install -r requirements.txt
#
# After installing, download the spaCy model the backend uses at startup:
#     python -m spacy download en_core_web_md   (preferred)
#     python -m spacy download en_core_web_sm   (smaller fallback)

# ── Backend: web framework ────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9      # required by FastAPI for File/Form uploads
python-dotenv>=1.0

# ── Backend: NLP / ML ─────────────────────────────────────────────────────────
spacy>=3.7,<4.0
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6
pandas>=2.2
scikit-learn>=1.4

# ── Backend: file parsing ─────────────────────────────────────────────────────
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
# Note: python-magic is NOT used — MIME detection is done via byte headers in resume_parser.py

# ── Backend: PDF report generation ───────────────────────────────────────────
reportlab>=4.0               # pure-Python PDF — no native libs needed
jinja2>=3.1

# ── Backend: external services ────────────────────────────────────────────────
httpx>=0.27                  # async Supabase REST client
groq>=0.13                   # Groq LLM client
PyJWT[crypto]>=2.8           # verifies Supabase JWT tokens via JWKS
certifi>=2024.2              # SSL cert bundle

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit>=1.32
requests>=2.31
supabase>=2.4                # frontend auth client (email/password + OAuth)
